export enum OverlayType {
  GENERIC = 'generic',
  LOADING = 'loading',
  CREATE_CASE = 'createCase',
  FILTER = 'filter',
  FILTER_TRANSACTION = 'filterTransaction',
  DUPLICATE = 'duplicate',
  POSITION_CRITERIA_SETTINGS = 'positionCriteriaSettings',
}
